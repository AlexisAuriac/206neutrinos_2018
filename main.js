// Generated by CoffeeScript 2.4.1
(function() {
  var a, dispRecords, h, handleInput, n, parser, rl, sd, updateRecords;

  parser = require("./src/argvParser.js");

  [n, a, h, sd] = parser(process.argv.slice(1));

  rl = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout,
    terminal: true
  });

  rl.setPrompt("Enter next value: ");

  updateRecords = function(line) {
    return ++n;
  };

  dispRecords = function() {
    return console.log(`\tNumber of values:	${n}\n\tStandard deviation	0\n\tArithmetic mean:	0\n\tRoot mean square:	0\n\tHarmonic mean:		0\n`);
  };

  handleInput = function(input) {
    if (input === "END") {
      process.exit(0);
    }
    updateRecords(input);
    dispRecords();
    return rl.prompt();
  };

  rl.prompt();

  rl.on('line', handleInput);

  // rl.on('close', () -> console.log "END")

}).call(this);
